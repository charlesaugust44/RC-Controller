void eempromInit() {
  loadConfig(0);
}

void saveConfig(int index) {
//  EEPROM.put(sizeof(config) * index, config);
}

void loadConfig(int index) {
 // EEPROM.get(sizeof(config) * index, config);
}
